#include "session.h"

int main(){
	// list available libraries
	// create socket to listen new connections
	
	game_server game;
	gsme.open_sessions = 0;
	
	while(1){
		// check new connections
		// if connected
		
		// check messages from exsisting clients
	}
}